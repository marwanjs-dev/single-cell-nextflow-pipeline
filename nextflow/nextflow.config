params {
    input = "pbmc3k"
    config_file = "$projectDir/../config/config.yml"
    outdir = "$projectDir/../results/nextflow_pca"
    model = "pca"
    n_components = 50
}

process {
    executor = 'local'
}